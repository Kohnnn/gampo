# UI/UX Wave — 2026-06-10

Scope: (0) land in-flight slots UX work, (1) arcade UI/UX unification (mobile-first), (2) slots mechanics/bonus/animation expansion, (3) cases unbox animation + UX. Executed by parallel agents with non-overlapping file ownership.

## Ownership boundaries (collision control)
- **Arcade agent**: `src/components/games/**` EXCLUDING `slots/`, `cases/`; MAY edit `primitives/BetPanel.jsx` + `primitives.css` (sole owner of primitives this wave).
- **Slots agent**: `src/components/games/slots/**`, `src/data/slotRtpScalars.js`, `scripts/calibrateSlots.mjs` only.
- **Cases agent**: `src/components/games/cases/**` only.
- Nobody edits `gameDefinitions.js` except the arcade agent (U7 thresholds).

## Gates (every phase)
- `npm test -- --run` green; `npm run build` clean
- Slots payout changes: `node scripts/calibrateSlots.mjs` then `node scripts/verifySlotRtp.mjs` within tolerance
- `node scripts/browserSmoke.mjs --routes=<changed> --viewports=390x844,466x704,492x820,1365x768` → 0 overflow / 0 console errors
- a11y: every new interactive element has an accessible name; reduced-motion fallback for every new animation

## Phase 0 — Land in-flight slots work
- [ ] P0-1 Replace old cycling Zap spin-mode button in the in-stage quick controls with `SpinModeControl` (compact); remove dead `cycleSpinMode`/`spinModeLabel` if unused
- [ ] P0-2 De-duplicate `data-slot-spin-mode` probe target (keep wrapper only)
- [ ] P0-3 Fix stale memo comment on `FeatureContractBody` (or memoize for real)
- [ ] P0-4 Gates + commit (slots UX commit separate from repo-audit files if any remain)

## Phase 1 — Arcade unification (mobile-first)
- [ ] U1 **War mobile pass**: war.css has zero `@media` rules; add mobile block + dock clearance. Surface tie Surrender on the mobile dock (new BetPanel `dockExtras` slot rendered inside the portaled dock during in-round).
- [ ] U2 **Dock-clearance utility**: shared `.stage-dock-clearance` (or token) using slots' formula `calc(var(--mobile-action-height,74px) + 18px)` + safe-area; apply to chickencross/tower/war (missing) and migrate the 17 hand-rolled copies opportunistically.
- [ ] U3 **StageActionButton primitive**: shared styled button for in-stage progression CTAs (blackjack bj-actions, videopoker vp-row, chickencross "Cross next", tower "Climb up", pump/drill/tome actions); 44px min hit target, accent-tinted, consistent focus.
- [ ] U4 **Legacy-7 baseline**: coinflip, rps, color, guess, lottery, sicbo, roulette get ResultToast + ActionLockOverlay + `useSfx` parity.
- [ ] U5 **Coinflip BigWinOverlay**: render the imported-but-unused overlay.
- [ ] U6 **useScrollActionIntoView** for keno, mines, plinko, tower, chickencross, baccarat.
- [ ] U7 **Centralize BigWin thresholds**: per-game `bigWinThreshold` in gameDefinitions (or local constants file) replacing 8 inline magic numbers.
- [ ] U8 **Critical-surface + breakpoint sweep**: add `data-mobile-critical-surface` to ~19 missing games; normalize one-off breakpoints (tomeoflife 466 → 480, tarot 500 → 480).

## Phase 2 — Slots mechanics / bonus / animation
- [ ] S1 **Cascade frames (foundation)**: `resolveSlotSpin` emits `cascadeFrames: [{cells, winCells, payout}]` per tumble step; UI animates real tumbles (symbols pop → fall → refill) instead of trace dots.
- [ ] S2 **Multiplier orbs** (Gates style): new `features.multiplierOrbs`; orb symbols carry 2–500×, sum applied to tumble-sequence total. Wire to 2 cascade templates.
- [ ] S3 **Wild multipliers**: `features.wildMultiplier` ×2/×3 on wild-substituted line wins; 2-3 templates.
- [ ] S4 **Random base-game features**: `features.randomFeature` — low-probability pre-eval injection of random wilds / wild reel with announce animation; 2 templates.
- [ ] S5 **Expanding special symbol** (Book style): FS variant picks a symbol that pays scatter-style across reels after expansion; 1 template.
- [ ] S6 **Walking wilds**: wild shifts one column per spin until exits, via existing sticky-wild threading; 1 template.
- [ ] S-anim **Animation pack**: tumble pop/fall/refill (needs S1); coin-shower particles on Big Win+; jackpot-tier celebration + screen shake; staggered scatter landing thuds; FS exit total-win count-up screen; hold&respin per-respin presentation from `respinLog`.
- [ ] S-cal Recalibrate + verify RTP for every touched template; cap fat tails via `maxWinMultiplier`; add/extend engine tests for each new mechanic.

## Phase 3 — Cases unbox
- [ ] C1 **rAF reel engine**: replace single CSS transition + 14 desynced tick timeouts with a rAF loop driving transform + per-tile-boundary tick callback (accurate tick audio).
- [ ] C2 **Overshoot + settle-back**: land 30–60px past target, ~350ms ease-back.
- [ ] C3 **Near-miss + suspense**: cosmetic-only seeding of rare tiles adjacent to target; deceleration-phase slowdown + heartbeat when a rare is adjacent.
- [ ] C4 **Pre-reveal rarity flash**: desaturate tiles during final approach; white→rarity-color flash on land so the result doesn't leak early.
- [ ] C5 **Multi-open layout**: remove 320px scroll cage for ×3/×5; staggered settles (~200ms apart); best-drop finale pulse + recap row.
- [ ] C6 **Mobile skip in dock** + fix `order:0` stragglers breaking reel-first hierarchy.
- [ ] C7 **Haptics**: `navigator.vibrate` on land/rare (throttled, setting-gated, reduced-motion off).

## Status board
| Task | Status | Notes |
|---|---|---|
| P0-1..4 | DONE | committed 73b5387f (slots UX) + 82a022a9 (repo audit); 453 tests green |
| U1 war mobile + dockExtras | DONE | b6adb227 — war.css mobile blocks, BetPanel dockExtras, Surrender on dock during tie |
| U2 dock clearance | DONE | b6adb227 — shared clearance via CoreStageFrame rule; hand-rolled paddings removed |
| U3 StageActionButton | DONE | 84e3baa3 — adopted in blackjack/chickencross/tower/pump/tomeoflife/drill; dead per-game CTA css removed; videopoker Draw already on BetPanel CTA (no change needed) |
| U4 legacy-7 baseline | DONE | 84e3baa3 — ResultToast+ActionLockOverlay+useSfx in coinflip/rps/color/guess/lottery/sicbo/roulette; stage roots given position:relative |
| U5 coinflip BigWin | DONE | 84e3baa3 |
| U6 scroll-into-view | DONE | 84e3baa3 — keno/mines/plinko/tower/chickencross/baccarat |
| U7 bigWinThresholds | DONE | 84e3baa3 — primitives/bigWinThresholds.js; all 33 games + cases use getBigWinThreshold(id) |
| U8 critical-surface + breakpoints | DONE | 84e3baa3 — 19 games tagged; tomeoflife 466→480, tarot 500→480 |
| Arcade smoke | DONE | 18 routes × 390x844/1365x768 = 36 combos, 0 overflow / 0 errors (output/browser-smoke/arcade-wave) |
| S1 cascade frames | ENGINE DONE / UI TODO | 172d4fed — resolveSlotSpin returns cascadeFrames; SlotsGame tumble animation + slotsMotion timings + tests still open |
| S2 multiplier orbs | todo | target templates: gummy-drops + bassline-bonus (cascade, High vol). Cascade templates: bassline-bonus, mummy-cascade, phoenix-megaways, mansion-megaways, gummy-drops |
| S3 wild multipliers | todo | |
| S4 random base features | todo | |
| S5/S6 expanding symbol / walking wilds | stretch | |
| S-anim | todo | scatter punch, FS exit count-up, jackpot tier, coin shower, respinLog presentation |
| S-cal | todo | required after any payout change: calibrateSlots.mjs → verifySlotRtp.mjs |
| C1–C7 | todo | cases unbox: rAF reel (reelMotion.js), overshoot, near-miss, rarity flash, multi-open cage removal, mobile skip pill, haptics |
| Integration gates | partial | tests 454 green, build clean as of 172d4fed |
| Deploy + live verify | todo | |

## Handoff notes (2026-06-10, session end)
- Branch `codex/gampo-polish-deploy`, all work committed through 172d4fed; tree clean except untracked helper scripts (scripts/runPreviewDetached.ps1, runSmokeDetached.ps1, listOrphanNode.ps1 — local conveniences, safe to delete or keep untracked).
- Subagent delegation produced empty reports in this environment; remaining phases should be implemented directly.
- Next step: S1 UI half — consume `result.cascadeFrames` in SlotsGame.jsx (pop → collapse → refill per frame, CASCADE_STEP_MS in slotsMotion.js, turbo halves, instant skips, reduced-motion jumps to final), then conservation test (frames replay to final cells).
- Cases C1: keep the exactly-once settlement guard in CasesGame.jsx intact when replacing the CSS-transition reel; total duration budget from casesAnimation.js exports.
- browserSmoke usage: `node scripts/browserSmoke.mjs --baseUrl=http://127.0.0.1:<port> --routes=/a,/b --viewports=390x844,1365x768 --label=<name>` against `npx vite preview`.
